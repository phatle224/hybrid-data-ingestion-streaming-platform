# CDC Server - Environment Configuration Template
# This file shows all required environment variables for the CDC pipeline
# 
# SETUP INSTRUCTIONS:
# 1. Copy to consumer/.env: cp .env.example consumer/.env
# 2. Update with your actual values
# 3. DO NOT commit consumer/.env to Git

# ===================================
# Kafka Configuration
# ===================================
KAFKA_BOOTSTRAP_SERVERS=kafka:9093
# Internal Docker network address
# Use kafka:9093 for containers, localhost:9092 for host

KAFKA_GROUP_ID=server-consumer-v14
# Consumer group ID - change if you need separate consumer groups

# ===================================
# MySQL Staging Database (Production)
# ===================================
MYSQL_HOST=localhost
# Production MySQL server IP/hostname

MYSQL_PORT=3306
# MySQL port

MYSQL_USER=synthetic_db_user
# MySQL user with permissions on staging database

MYSQL_PASSWORD=REDACTED_PASSWORD
# MySQL password (keep secure!)

MYSQL_DATABASE=insuranceWarehouse
# Staging database name

# ===================================
# Topic Prefix (MUST match Debezium connector)
# ===================================
TOPIC_PREFIX=server.insuranceSale
# Must match "topic.prefix" in register-mysql-final.json
# Topics created: {prefix}.contract, {prefix}.contractObject, {prefix}.claim

# ===================================
# Python Consumer Configuration
# ===================================
LOG_LEVEL=INFO
# Options: DEBUG, INFO, WARNING, ERROR
# Use DEBUG for troubleshooting (very verbose)

CONSUMER_TIMEOUT_MS=1000
# Time to wait for messages before timeout

MAX_POLL_INTERVAL_MS=300000
# Maximum interval between polls (5 minutes)

MAX_POLL_RECORDS=500
# Maximum records to fetch per poll

AUTO_OFFSET_RESET=earliest
# Options: earliest, latest, none
# earliest = read from beginning if no offset stored
# latest = only read new messages

# ===================================
# NOTES
# ===================================
# - Keep this file updated when configuration changes
# - Never commit actual .env file to Git (it's in .gitignore)
# - TOPIC_PREFIX must match Debezium connector configuration
# - KAFKA_GROUP_ID should be unique to avoid consumer conflicts
